# Pause Overlay (`pause_overlay.png`)

Backdrop tint behind any modal (settings, tech tree). Used as a full-screen
darken layer.

- Should be near-black with low alpha (~60%); the goal is to gently dim
  the world without hiding context.
- Layered behind `settings_menu.png` / `tech_tree.png`.
