# World-Map Site Tooltip (`site_tooltip.png`)

Hover tooltip that appears over a site on the world map. Lives in
`scenes/world/WorldMap.tscn` (`TooltipLayer/SiteTooltip/Panel`).

- Compact — width follows content.
- Tone: travel-itinerary chip; readable against the world map background.
