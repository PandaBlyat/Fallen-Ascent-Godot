# Worker Roster Grid (`roster_panel.png`)

Inner grid panel inside the selection panel that lists all selected
workers as cards. Built in `scripts/ui/ColonyHud.gd` (`roster_panel`,
around line 979).

- Lives inside `selection_panel.png`.
- Slightly raised relative to the parent panel.
- Hosts a grid of `worker_card.png` entries.
