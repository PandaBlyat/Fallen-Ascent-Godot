# NPC Roster Strip (`npc_strip.png`)

Tight roster strip showing the colony's bots as portraits with mood bars.
Built in `scripts/ui/ColonyHud.gd` (`NpcStrip`, around line 247).

- Anchored: top-center, below `top_strip`.
- Width ~`WORKER_LIST_WIDTH` (260 px), height ~`WORKER_LIST_HEIGHT` (126).
- Tone: same family as top_strip but slightly raised so the worker
  portraits read against the world.
