# Worker Entry Card (`worker_card.png`)

Single worker card inside the roster panel — portrait, name, mood bar,
quick stats. Built in `scripts/ui/ColonyHud.gd` (`card_panel`, around
line 1011).

- Width ~`WORKER_CARD_WIDTH` (560 px), variable height.
- Tone: should pop slightly above `roster_panel.png` so each card is
  scannable; consider a colored top edge for status.
