# Tech Tree Dialog (`tech_tree.png`)

Background of the full-screen tech tree modal. Built in
`scripts/ui/TechTreePanel.gd` (around line 40). Hosts the tech node tiles
(`tech_node.png`).

- Modal — covers most of the viewport (~920 x 600 around screen center).
- Tone: deep, "console terminal" feel; should let `tech_node.png` tiles
  glow against it.
