# Tech Node Tile (`tech_node.png`)

Individual tech entry inside the tech tree dialog. Built in
`scripts/ui/TechTreePanel.gd._make_tech_button` (around line 141).

- ~160 x 80 with rounded corners.
- Should support tinted state overlays for locked / available / researched
  (handled in code via modulate/style swaps).
