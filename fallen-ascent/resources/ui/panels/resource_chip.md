# Resource Chip (`resource_chip.png`)

Small pill behind a resource icon + count in the top strip. Built in
`scripts/ui/ColonyHud.gd` (`chip`, around line 1106).

- Tiny: ~24 px tall.
- Tone: low-contrast against `top_strip.png`; the icon and number do
  the work.
