# History / Event Log Strip (`history_log.png`)

Reserved slot for a future scrolling event/history strip (combat log,
notable colony events). Not yet wired into the HUD — drop your texture
in here and the next pass will wire it up.

- Plan: ~`SELECTION_PANEL_WIDTH` wide, short height; bottom-left or
  bottom-right of screen.
