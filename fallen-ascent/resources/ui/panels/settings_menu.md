# Settings Menu (`settings_menu.png`)

Background of the in-game settings modal opened via Esc. Lives in
`scenes/ui/SettingsMenu.tscn`.

- Modal — anchored to screen center, fixed size.
- Tone: heaviest "interrupt" panel in the UI; should clearly read as
  a pause-the-game dialog.
